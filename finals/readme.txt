1. Setup
Create a virtual environment with Python 3.10, activate. 
Install requirements: pip install -r requirements.txt
Install postgresql
bash setup_databases.sh
2. Run airflow
bash run_airflow.sh
Go to localhost:8080 and run stock_data_pipeline and stock_model_pipeline.

